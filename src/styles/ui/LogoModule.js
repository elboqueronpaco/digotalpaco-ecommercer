import { Link } from 'react-router-dom'
import styled from 'styled-components'

export const LogoModule = styled(Link)`
    display: flex;
    justify-content: center;
    align-items: center;
    svg{
        width: 50px;
    }
`